server:
  port: 8080

spring:
  application:
    name: maven-tool-plugin
  mvc:
    static-path-pattern: /**
  resources:
    static-locations: classpath:/,classpath:/
  profiles:
    active: dev

defoliation:
  resources:
    locations:
      - /templates
      - /static

  object:
